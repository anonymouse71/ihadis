<div class="second-nav-holder book-title-holder">

    <div class="fl title-holder" id="hadis-navbar">
        {% if app.request.cookies.has('lang') and app.request.cookies.get('lang') == 'en' %}
            <h1>{{ book.titleEnglish ? book.titleEnglish : book.title }}</h1>
        {% else %}
            <h1 class="bn">{{ book.title }}</h1>
        {% endif %}
    </div>

    <div class="other-nav">
        <h1 style="margin-top: 10px;">{{ book.titleArabic }}</h1>
    </div>

    <div class="clr"></div>

</div>